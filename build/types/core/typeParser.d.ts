import * as T from "../../types";
export default class TypeParser {
    static parseBoolean(envInfo: T.EnvInfo): void;
    static parseNumber(envInfo: T.EnvInfo): void;
    static parseJson(envInfo: T.EnvInfo): void;
}
